let search = document.getElementById();

search.addEventListener('click',(e)=>{

} )

let nav = document.getElementById();

nav.onclick = function(){
   let nav_bar = document.getElementById();
   let display = nav_bar.style.display;
   if(display === 'none'){
        display = 'flex';
        nav_bar.style.direction ='row';

   }else{
        display = 'none';
   }
}